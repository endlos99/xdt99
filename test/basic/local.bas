 REM LOCAL LABELS
START:
 R=RND
 CALL D
 CALL A
 CALL B(1)
 CALL C("FOO")
 GOTO LABEL

 SUB A
 CALL CLEAR
%LABEL:
 IF R>.9 THEN %LABEL
 SUBEND
 SUB B(X)
 Y=X
%LABEL:
 IF Y*R<X THEN %LABEL
 SUBEND :: SUB C(X$)
 X=LEN(X$)
%LABEL:
 IF X*R>10 THEN %LABEL
 SUBEND :: SUB D :: CALL CLEAR :: SUBEND

LABEL:
 END
